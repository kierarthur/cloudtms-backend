# Disposable container only, published exclusively on Windows loopback.
# Docker bridge gateway was verified before this local fixture was installed.
local all postgres trust
host all postgres 127.0.0.1/32 trust
host all postgres 172.17.0.1/32 trust
host all postgres ::1/128 trust
