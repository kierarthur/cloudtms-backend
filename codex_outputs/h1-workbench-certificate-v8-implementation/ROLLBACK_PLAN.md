# H1 V8 rollback

**LOCAL CANDIDATE — NOT INSTALLED — NOT DEPLOYED**

Rollback is an evidence-preserving forward release, never a destructive table or business-data deletion.

1. Disable new certificate admission by restoring the exact prior normal TEST Worker from backend base `16b299bf03c3d752f6dbd7891c0d23044e88e890`.
2. Reassert the exact pre-V8 Workbench function definitions and generated contract from that same source. Preserve the already-installed H1 causal-recovery correction; do not revert it.
3. Revoke service execution of V8 public entry points through the superseding rollback repeatable while retaining logical-owner access for audit.
4. Retain all V8 certificate tables, rows, links, lifecycle events, page receipts and attestations. Do not delete Workbench, Draft, job, payment or audit history.
5. Restore frontend `278dcbd007bb3cb6a13d09b5fe0d8aaf5132a5be` only if the V8 status fields are withdrawn. The frontend's fail-closed behaviour is compatible with a missing certificate contract but its polling copy should remain version-aligned with the Worker.
6. Verify exact prior function identities, signatures, owners, ACLs, security, search paths, statement/lock budgets and prior contract SHA-256 `35f33db2ee73689d72106fa90cfc250d0ad587051704af335f405dad1fc6946e`.
7. Never downgrade a certificate-bound operation silently. An existing FROZEN operation retains its immutable link; an operation that has not reached FROZEN is stopped safely and retained as audit evidence.
8. Run the same status, admission, tamper, response-loss, queue, contract and browser checks after reversal. Confirm no Draft/provider/payment action and no data removal.

Local rollback evidence currently consists of exact old-source versus candidate contract comparison, PostgreSQL 17/18 replacement/reapply evidence and rollback-contained first-use fixtures. Before any shared TEST reversal, regenerate the rollback contract from the then-current TEST base and independently compare installed definitions after the managed Miget release.
