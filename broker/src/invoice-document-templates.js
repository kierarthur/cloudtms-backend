const ESCAPE = Object.freeze({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' });
export function escapeInvoiceDocumentHtml(value) { return String(value ?? '').replace(/[&<>"']/g, character => ESCAPE[character]); }
export const escapeInvoiceHtml = escapeInvoiceDocumentHtml;

export function formatFrozenDocumentValue(value, options = {}) {
  if (value == null || value === '') return options.empty ?? '';
  if (options.kind === 'money') {
    const number = Number(value); if (!Number.isFinite(number)) return '';
    return new Intl.NumberFormat('en-GB', { style: 'currency', currency: options.currency || 'GBP', minimumFractionDigits: 2, maximumFractionDigits: 2 }).format(number);
  }
  if (options.kind === 'number') {
    const number = Number(value); if (!Number.isFinite(number)) return '';
    return new Intl.NumberFormat('en-GB', { minimumFractionDigits: options.minimumFractionDigits ?? 0, maximumFractionDigits: options.maximumFractionDigits ?? 2 }).format(number);
  }
  if (options.kind === 'date') {
    const match = /^(\d{4})-(\d{2})-(\d{2})/.exec(String(value));
    return match ? `${match[3]}/${match[2]}/${match[1]}` : String(value);
  }
  return String(value);
}

function renderAddress(address) {
  if (!address) return '';
  const lines = Array.isArray(address) ? address : [address.name, address.line1 || address.address_line_1, address.line2 || address.address_line_2, address.line3 || address.address_line_3, address.city || address.town, address.county, address.postcode, address.country];
  return lines.filter(Boolean).map(line => `<div>${escapeInvoiceHtml(line)}</div>`).join('');
}

export function buildInvoiceTemplateCss() {
  return `@page{size:A4;margin:14mm 13mm 16mm}*{box-sizing:border-box}html,body{margin:0;padding:0;color:#172033;font:10px/1.35 Arial,Helvetica,sans-serif}body{-webkit-print-color-adjust:exact;print-color-adjust:exact}.document{position:relative;width:100%}.draft-watermark{position:fixed;inset:34% 0 auto;transform:rotate(-28deg);text-align:center;font-size:76px;font-weight:700;color:rgba(153,27,27,.12);z-index:-1;letter-spacing:8px}.brand{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:16px;border-bottom:3px solid #153a67;padding-bottom:8px;margin-bottom:12px}.brand img{max-width:180px;max-height:62px;object-fit:contain}.brand-name{color:#153a67;font-size:18px;font-weight:700}.document-kind{color:#153a67;font-size:24px;font-weight:800;text-align:right}.identity,.references,.meta{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:6px;margin-bottom:12px}.field,.party,.payment,.legal,.verification{border:1px solid #d7deea;border-radius:4px;padding:7px}.label,.party-title{color:#5a6578;font-size:8px;font-weight:700;text-transform:uppercase;letter-spacing:.45px}.value{margin-top:3px;font-weight:600;overflow-wrap:anywhere}.parties{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:12px}.party{min-height:100px}.party-title{color:#153a67;margin-bottom:5px}table{width:100%;border-collapse:collapse;table-layout:fixed}thead{display:table-header-group}tr{break-inside:avoid}th{background:#153a67;color:#fff;padding:6px 5px;font-size:8px;text-align:left;text-transform:uppercase}td{border-bottom:1px solid #e0e5ee;padding:6px 5px;vertical-align:top;overflow-wrap:anywhere}.number{text-align:right}.summary-grid{display:grid;grid-template-columns:minmax(0,1fr) 245px;gap:14px;margin-top:14px;break-inside:avoid}.vat-table th{background:#e9eef6;color:#172033}.totals{border:1px solid #cbd4e2;border-radius:5px;overflow:hidden}.total-row{display:flex;justify-content:space-between;padding:7px 9px;border-bottom:1px solid #e0e5ee}.total-row.grand{background:#153a67;color:white;font-size:13px;font-weight:800}.payment,.legal,.verification{margin-top:12px}.legal{color:#525d70;font-size:8px}.attachment-index,.source-page{break-before:page}.attachment-index h1,.source-page h1{color:#153a67;font-size:20px}.separator{height:245mm;display:flex;flex-direction:column;justify-content:center;align-items:center;text-align:center;border:4px solid #153a67}.separator h1{font-size:30px;color:#153a67}.signature-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:12px}.signature img{max-width:180px;max-height:70px}.subheading{color:#153a67;font-size:13px;margin:12px 0 6px}`;
}

function field(label, value, kind) { return `<div class="field"><div class="label">${escapeInvoiceHtml(label)}</div><div class="value">${escapeInvoiceHtml(formatFrozenDocumentValue(value,{kind}))}</div></div>`; }
function docLabel(model) { return model.document_label || (model.credit_note || model.document_type === 'CREDIT_NOTE' ? 'CREDIT NOTE' : model.self_bill || model.document_type === 'SELF_BILL_INVOICE' ? 'SELF-BILL INVOICE' : 'INVOICE'); }

export function renderInvoiceBrandHeader(model = {}) {
  const supplier = model.supplier || {};
  const logo = model.branding?.logo_data_url || '';
  return `<header class="brand"><div>${logo ? `<img alt="" src="${escapeInvoiceHtml(logo)}">` : ''}<div class="brand-name">${escapeInvoiceHtml(supplier.legal_name || supplier.trading_name || supplier.name || '')}</div></div><div class="document-kind">${escapeInvoiceHtml(docLabel(model))}</div></header>`;
}
export function renderInvoiceIdentityPanel(model = {}) {
  return `<section class="identity">${field(model.credit_note ? 'Credit note number' : 'Invoice number',model.invoice_number || model.invoice_no)}${field('Issue date',model.issue_date,'date')}${field('Tax point',model.tax_point || model.tax_point_date,'date')}${field('Due date',model.due_date,'date')}</section>`;
}
export function renderInvoicePartyBlocks(model = {}) {
  const supplier = model.supplier || {}; const customer = model.customer || model.client || {};
  const supplierMeta = [supplier.legal_name, supplier.trading_name && supplier.trading_name !== supplier.legal_name ? `Trading as: ${supplier.trading_name}` : null, supplier.company_registration_number ? `Company no: ${supplier.company_registration_number}` : null, supplier.vat_registration_number ? `VAT no: ${supplier.vat_registration_number}` : null, supplier.contact_email, supplier.contact_phone].filter(Boolean);
  const customerMeta = [customer.legal_name || customer.name, customer.account_reference ? `Account: ${customer.account_reference}` : null].filter(Boolean);
  return `<section class="parties"><div class="party"><div class="party-title">Supplier</div>${supplierMeta.map(v=>`<div>${escapeInvoiceHtml(v)}</div>`).join('')}${renderAddress(supplier.registered_address || supplier.address)}</div><div class="party"><div class="party-title">Bill to</div>${customerMeta.map(v=>`<div>${escapeInvoiceHtml(v)}</div>`).join('')}${renderAddress(customer.billing_address || customer.address)}</div></section>`;
}
export function renderInvoiceReferencePanel(model = {}) {
  const references = model.references || {};
  return `<section class="references">${field('Purchase order',references.purchase_order || model.purchase_order)}${field('Client reference',references.client_reference || model.client_reference)}${field('Work location',references.work_location || model.work_location || model.site)}${field('Candidate / worker',model.candidate_summary || model.worker_summary)}${field('Payment terms',model.payment_terms_text || model.payment?.terms_text)}${field('Currency',model.currency || 'GBP')}${model.credit_note ? field('Original invoice',model.original_invoice_number) + field('Original invoice date',model.original_invoice_date,'date') : ''}</section>${model.credit_note_reason ? `<section class="payment"><strong>Credit reason:</strong> ${escapeInvoiceHtml(model.credit_note_reason)}</section>` : ''}`;
}
export function renderInvoiceLineTable(model = {}) {
  const currency=model.currency||'GBP'; const rows=Array.isArray(model.lines)?model.lines:[];
  return `<table><thead><tr><th>Description</th><th>Reference</th><th>Unit</th><th class="number">Qty</th><th class="number">Rate</th><th class="number">Net</th><th class="number">VAT</th><th class="number">Gross</th></tr></thead><tbody>${rows.map(line=>`<tr><td>${escapeInvoiceHtml(line.description||line.label||'')}</td><td>${escapeInvoiceHtml(line.reference||'')}</td><td>${escapeInvoiceHtml(line.unit||line.units||'')}</td><td class="number">${escapeInvoiceHtml(formatFrozenDocumentValue(line.quantity,{kind:'number',maximumFractionDigits:4}))}</td><td class="number">${escapeInvoiceHtml(formatFrozenDocumentValue(line.unit_price??line.rate,{kind:'money',currency}))}</td><td class="number">${escapeInvoiceHtml(formatFrozenDocumentValue(line.net_amount??line.net,{kind:'money',currency}))}</td><td class="number">${escapeInvoiceHtml(formatFrozenDocumentValue(line.vat_amount??line.vat,{kind:'money',currency}))}</td><td class="number">${escapeInvoiceHtml(formatFrozenDocumentValue(line.gross_amount??line.gross,{kind:'money',currency}))}</td></tr>`).join('')}</tbody></table>`;
}
export function renderInvoiceVatBreakdown(model={}) { const currency=model.currency||'GBP'; const rows=Array.isArray(model.vat_breakdown)?model.vat_breakdown:[]; return rows.length?`<table class="vat-table"><thead><tr><th>VAT rate</th><th class="number">Net</th><th class="number">VAT</th><th class="number">Gross</th></tr></thead><tbody>${rows.map(row=>`<tr><td>${escapeInvoiceHtml(formatFrozenDocumentValue(row.rate??row.vat_rate,{kind:'number'}))}%</td><td class="number">${escapeInvoiceHtml(formatFrozenDocumentValue(row.net_amount??row.net,{kind:'money',currency}))}</td><td class="number">${escapeInvoiceHtml(formatFrozenDocumentValue(row.vat_amount??row.vat,{kind:'money',currency}))}</td><td class="number">${escapeInvoiceHtml(formatFrozenDocumentValue(row.gross_amount??row.gross,{kind:'money',currency}))}</td></tr>`).join('')}</tbody></table>`:''; }
export function renderInvoiceTotalsPanel(model={}) { const t=model.totals||{}; const currency=model.currency||'GBP'; const row=(label,value,css='')=>`<div class="total-row ${css}"><span>${escapeInvoiceHtml(label)}</span><span>${escapeInvoiceHtml(formatFrozenDocumentValue(value,{kind:'money',currency}))}</span></div>`; return `<div class="totals">${row('Net',t.net??model.net_total)}${row('VAT',t.vat??model.vat_total)}${row('Gross',t.gross??model.gross_total)}${row('Amount paid',t.amount_paid??model.amount_paid??0)}${row('Amount credited',t.amount_credited??model.amount_credited??0)}${row('Amount outstanding',t.amount_outstanding??model.amount_outstanding??t.amount_due??model.amount_due??t.gross??model.gross_total,'grand')}</div>`; }
export function renderInvoicePaymentPanel(model={}) { const p=model.payment||{}; return Object.values(p).some(Boolean)?`<section class="payment"><div class="party-title">Payment and remittance</div>${p.terms_text?`<div>Terms: ${escapeInvoiceHtml(p.terms_text)}</div>`:''}${p.due_date_basis?`<div>Due-date basis: ${escapeInvoiceHtml(p.due_date_basis)}</div>`:''}${p.instructions?`<div>${escapeInvoiceHtml(p.instructions)}</div>`:''}${p.account_name?`<div>Account name: ${escapeInvoiceHtml(p.account_name)}</div>`:''}${p.sort_code?`<div>Sort code: ${escapeInvoiceHtml(p.sort_code)}</div>`:''}${p.account_number?`<div>Account number: ${escapeInvoiceHtml(p.account_number)}</div>`:''}${p.remittance_reference?`<div>Reference: ${escapeInvoiceHtml(p.remittance_reference)}</div>`:''}${p.remittance_email?`<div>Remittance: ${escapeInvoiceHtml(p.remittance_email)}</div>`:''}</section>`:''; }
export function renderInvoiceLegalFooter(model={}) { const s=model.supplier||{}; const lines=Array.isArray(model.legal_wording)?model.legal_wording:[model.self_bill_legal_wording,model.legal_wording||model.legal_footer].filter(Boolean); const identity=[s.legal_name,s.company_registration_number?`Company ${s.company_registration_number}`:null,s.vat_registration_number?`VAT ${s.vat_registration_number}`:null,model.template_version?`Template ${model.template_version}`:null].filter(Boolean).join(' · '); return `<footer class="legal">${lines.map(line=>`<div>${escapeInvoiceHtml(line)}</div>`).join('')}${identity?`<div>${escapeInvoiceHtml(identity)}</div>`:''}</footer>`; }

export function renderInvoiceAttachmentIndex(entries=[]) { return Array.isArray(entries)&&entries.length?`<section class="attachment-index"><h1>Attachment index</h1><table><thead><tr><th>No.</th><th>Worker / source</th><th>Week / date</th><th>Document type</th><th>Evidence</th><th>Reference</th><th class="number">Start</th><th class="number">Pages</th></tr></thead><tbody>${entries.map((e,i)=>`<tr data-row-id="${escapeInvoiceHtml(e.row_id||'')}"><td>${escapeInvoiceHtml(e.attachment_number??i+1)}</td><td>${escapeInvoiceHtml(e.worker||e.source||'')}</td><td>${escapeInvoiceHtml(formatFrozenDocumentValue(e.week_or_date,{kind:'date'}))}</td><td>${escapeInvoiceHtml(e.document_type||'')}</td><td>${escapeInvoiceHtml(e.evidence_description||'')}</td><td>${escapeInvoiceHtml(e.reference||'')}</td><td class="number">${escapeInvoiceHtml(e.start_page??'')}</td><td class="number">${escapeInvoiceHtml(e.page_count??'')}</td></tr>`).join('')}</tbody></table></section>`:''; }
export function renderInvoicePageNumbering() { return ''; }

export function buildProfessionalInvoiceHtml(model={}) { const m=model&&typeof model==='object'?model:{}; return `<!doctype html><html lang="en"><head><meta charset="utf-8"><style>${buildInvoiceTemplateCss()}</style></head><body>${m.is_draft||m.purpose==='DRAFT_PREVIEW'?'<div class="draft-watermark">DRAFT</div>':''}<main class="document">${renderInvoiceBrandHeader(m)}${renderInvoiceIdentityPanel(m)}${renderInvoicePartyBlocks(m)}${renderInvoiceReferencePanel(m)}${renderInvoiceLineTable(m)}<section class="summary-grid"><div>${renderInvoiceVatBreakdown(m)}</div>${renderInvoiceTotalsPanel(m)}</section>${renderInvoicePaymentPanel(m)}${renderInvoiceLegalFooter(m)}${m.attachment_index_inline?renderInvoiceAttachmentIndex(m.attachment_index||[]):''}</main></body></html>`; }

function signatureBlock(label,signature={}) { const src=signature.data_url||''; return `<div class="signature"><div class="party-title">${escapeInvoiceHtml(label)}</div>${src?`<img alt="" src="${escapeInvoiceHtml(src)}">`:''}<div>${escapeInvoiceHtml(signature.name||signature.identity||'')}</div><div>${escapeInvoiceHtml(signature.role||'')}</div></div>`; }
function scheduleRows(rows=[]) { return `<table><thead><tr><th>Date</th><th>Scheduled</th><th>Worked</th><th>Break</th><th>Reference</th><th class="number">Hours / units</th></tr></thead><tbody>${rows.map(row=>`<tr><td>${escapeInvoiceHtml(formatFrozenDocumentValue(row.date,{kind:'date'}))}</td><td>${escapeInvoiceHtml([row.scheduled_start,row.scheduled_end].filter(Boolean).join(' – '))}</td><td>${escapeInvoiceHtml([row.worked_start||row.start,row.worked_end||row.end].filter(Boolean).join(' – '))}</td><td>${escapeInvoiceHtml(row.break_display||[row.break_start,row.break_end,row.break_minutes!=null?`${row.break_minutes} min`:null].filter(Boolean).join(' / '))}</td><td>${escapeInvoiceHtml(row.reference||row.day_reference||'')}</td><td class="number">${escapeInvoiceHtml(formatFrozenDocumentValue(row.hours??row.units,{kind:'number'}))}</td></tr>`).join('')}</tbody></table>`; }
export function buildElectronicTimesheetHtml(model={}) { const rows=Array.isArray(model.daily_schedule_rows)?model.daily_schedule_rows:(Array.isArray(model.rows||model.entries)?model.rows||model.entries:[]); const auth=model.authorisation||{}; return `<section class="source-page"><h1>Electronic timesheet</h1><div class="meta">${field('Candidate',model.candidate_name||model.candidate?.name)}${field('Candidate ID',model.candidate_identifier||model.candidate?.id)}${field('Client',model.client_name)}${field('Contract',model.contract_reference||model.contract_identity)}${field('Hospital / site',model.hospital||model.site)}${field('Ward',model.ward)}${field('Assignment / job',model.assignment||model.job_title)}${field('Band / shift type',[model.band,model.shift_type].filter(Boolean).join(' / '))}${field('Week ending',model.week_ending_date,'date')}${field('Submission mode',model.submission_mode)}${field('Sheet scope',model.sheet_scope)}${field('Document revision',model.timesheet_document_revision)}</div>${scheduleRows(rows)}${Array.isArray(model.weekly_schedule_rows)&&model.weekly_schedule_rows.length?`<h2 class="subheading">Weekly schedule</h2>${scheduleRows(model.weekly_schedule_rows)}`:''}<section class="verification"><div>Whole reference: ${escapeInvoiceHtml(model.whole_timesheet_reference||'')}</div><div>Segment references: ${escapeInvoiceHtml((model.segment_references||[]).join(', '))}</div><div>Additional units: ${escapeInvoiceHtml(formatFrozenDocumentValue(model.additional_units,{kind:'number'}))}</div><div>QR required: ${model.qr_required?'Yes':'No'} · QR signed: ${model.qr_signed?'Yes':'No'}</div><div>${escapeInvoiceHtml(model.qr_signature_verification_summary||'')}</div><div>Authorised: ${model.authorised?'Yes':'No'} ${escapeInvoiceHtml(auth.authorised_at||model.authorised_at||'')} by ${escapeInvoiceHtml(auth.name||model.authoriser_name||'')} ${escapeInvoiceHtml(auth.role||model.authoriser_role||'')}</div></section><div class="signature-grid">${signatureBlock('Candidate / nurse signature',model.candidate_signature||model.nurse_signature)}${signatureBlock('Authoriser signature',model.authoriser_signature)}</div></section>`; }
export function buildAttachmentIndexHtml(model={}) { return `<section class="source-page">${renderInvoiceAttachmentIndex(model.display_rows||model.entries||[])}</section>`; }
export function buildSectionSeparatorHtml(model={}) { return `<section class="separator"><h1>${escapeInvoiceHtml(model.title||model.label||'Supporting document')}</h1>${model.subtitle?`<p>${escapeInvoiceHtml(model.subtitle)}</p>`:''}${model.reference?`<p>Reference: ${escapeInvoiceHtml(model.reference)}</p>`:''}</section>`; }
function supportTable(title,model,columns) { const rows=Array.isArray(model.rows)?model.rows:[]; return `<section class="source-page"><h1>${escapeInvoiceHtml(title)}</h1><table><thead><tr>${columns.map(c=>`<th>${escapeInvoiceHtml(c.label)}</th>`).join('')}</tr></thead><tbody>${rows.map(row=>`<tr>${columns.map(c=>`<td>${escapeInvoiceHtml(formatFrozenDocumentValue(row?.[c.key],{kind:c.kind}))}</td>`).join('')}</tr>`).join('')}</tbody></table></section>`; }
export function buildHealthRosterSupportHtml(model={}) { return supportTable('HealthRoster support',model,[['worker','Worker'],['assignment','Assignment'],['shift_date','Date','date'],['shift_times','Shift'],['site','Site'],['ward','Ward'],['reference','Reference'],['units_hours','Units / hours'],['validation_state','Validation'],['source_identity','Source']].map(([key,label,kind])=>({key,label,kind}))); }
export function buildNhspSupportHtml(model={}) { return supportTable('NHSP support',model,[['worker','Worker'],['nhsp_shift_id','Shift ID'],['booking_reference','Booking / reference'],['site_ward','Site / ward'],['shift_date','Date','date'],['shift_times','Times'],['hours_units','Hours / units'],['source_identity','Source'],['validation_state','Validation']].map(([key,label,kind])=>({key,label,kind}))); }
export function buildHigherRateSupportHtml(model={}) { return supportTable('Higher-rate support',model,[['worker_source','Worker / source'],['shift_date','Date / shift'],['original_rate','Original rate'],['applied_rate','Applied rate'],['units','Units'],['display_amount','Amount'],['reason','Reason'],['approval_identity','Approval'],['reference','Reference']].map(([key,label,kind])=>({key,label,kind}))); }
export function buildInvoiceSourceDocumentHtml(renderKind,model={}) { const kind=String(renderKind||model.render_kind||'').toUpperCase(); const builder={ELECTRONIC_TIMESHEET:buildElectronicTimesheetHtml,ATTACHMENT_INDEX:buildAttachmentIndexHtml,SECTION_SEPARATOR:buildSectionSeparatorHtml,HEALTHROSTER_SUPPORT:buildHealthRosterSupportHtml,NHSP_SUPPORT:buildNhspSupportHtml,HIGHER_RATE_SUPPORT:buildHigherRateSupportHtml}[kind]; if(!builder) throw new Error(`UNSUPPORTED_INVOICE_SOURCE_RENDER_KIND:${kind||'EMPTY'}`); return `<!doctype html><html lang="en"><head><meta charset="utf-8"><style>${buildInvoiceTemplateCss()}</style></head><body>${builder(model)}</body></html>`; }